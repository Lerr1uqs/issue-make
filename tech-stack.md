# 🛠️ Issue-Make 技术栈详细说明

## 核心技术选型

### 运行时环境
- **Node.js >= 16.0.0**: 提供现代JavaScript/TypeScript运行时支持
- **TypeScript 5.2+**: 类型安全的开发体验
- **pnpm**: 高效的包管理器（可选，支持npm/yarn）

### 核心依赖说明

#### 🖥️ 用户界面层
```json
{
  "ink": "^4.4.1",               // React for CLI - 终端UI框架
  "react": "^18.2.0",            // React核心库
  "commander": "^11.1.0",        // CLI命令解析
  "inquirer": "^8.2.6",          // 交互式命令行界面
  "chalk": "^4.1.2"              // 终端颜色输出
}
```

#### 🤖 AI集成层
```json
{
  "openai": "^4.20.1"            // OpenAI SDK，支持所有兼容服务商
}
```

#### 📁 文件系统层
```json
{
  "fs-extra": "^11.1.1",         // 增强的文件系统操作
  "yaml": "^2.3.4",              // YAML解析和生成
  "glob": "^10.3.10"             // 文件模式匹配
}
```

#### 🔧 工具库
```json
{
  "date-fns": "^2.30.0",         // 日期处理
  "dotenv": "^16.3.1"            // 环境变量管理
}
```

### 开发工具链

#### 代码质量
```json
{
  "@typescript-eslint/eslint-plugin": "^6.12.0",
  "@typescript-eslint/parser": "^6.12.0",
  "eslint": "^8.54.0",
  "eslint-config-prettier": "^9.0.0",
  "eslint-plugin-prettier": "^5.0.1",
  "prettier": "^3.1.0"
}
```

#### 测试框架
```json
{
  "jest": "^29.7.0",
  "ts-jest": "^29.1.1",
  "@types/jest": "^29.5.8",
  "ink-testing-library": "^3.0.1",
  "@types/react": "^18.2.0"
}
```

#### 构建工具
```json
{
  "typescript": "^5.2.2",
  "ts-node": "^10.9.1",
  "rimraf": "^5.0.5"
}
```

## 架构层次设计

```
┌─────────────────────────────────────────┐
│              用户接口层                   │
├─────────────────┬───────────────────────┤
│    TUI界面      │      CLI命令行         │
│   (ink/React)   │    (commander)         │
└─────────────────┴───────────────────────┘
┌─────────────────────────────────────────┐
│              业务逻辑层                   │
├─────────────────┬───────────────────────┤
│   Issue管理     │     AI服务集成         │
│   文件流转      │    配置管理             │
└─────────────────┴───────────────────────┘
┌─────────────────────────────────────────┐
│              基础设施层                   │
├─────────────────┬───────────────────────┤
│   文件系统      │      工具函数           │
│ (fs-extra)      │   (date-fns等)         │
└─────────────────┴───────────────────────┘
```

## 关键技术决策

### 1. 为什么选择TypeScript？
- **类型安全**: 在编译时捕获错误，减少运行时问题
- **开发体验**: 优秀的IDE支持和自动补全
- **可维护性**: 明确的接口定义和文档化
- **生态系统**: 丰富的类型定义包

### 2. 为什么选择Ink？
- **React生态**: 基于React，利用成熟的组件化开发模式
- **类型安全**: 完美的TypeScript支持
- **测试友好**: 配合ink-testing-library便于TUI组件测试
- **社区活跃**: 广泛使用，文档和示例丰富
- **现代化**: 支持Hooks、Context等现代React特性

### 3. 为什么选择OpenAI SDK？
- **通用性**: 支持所有OpenAI兼容的服务商
- **稳定性**: 官方维护，更新及时
- **功能完整**: 支持流式输出、重试机制等
- **社区活跃**: 丰富的文档和示例

### 4. 为什么选择Commander.js？
- **成熟稳定**: 广泛使用的CLI框架
- **功能完整**: 支持子命令、参数验证、帮助生成
- **TypeScript支持**: 原生类型定义
- **生态兼容**: 与其他CLI工具良好集成

## 性能优化策略

### 1. 构建优化
```typescript
// tsconfig.json 优化配置
{
  "compilerOptions": {
    "target": "ES2020",           // 现代JS特性
    "module": "commonjs",         // Node.js兼容
    "skipLibCheck": true,         // 跳过库文件检查加速编译
    "incremental": true,          // 增量编译
    "tsBuildInfoFile": ".tsbuildinfo"
  }
}
```

### 2. 运行时优化
- **懒加载**: AI服务按需初始化
- **缓存机制**: 配置文件和目录扫描结果缓存
- **异步操作**: 文件I/O使用Promise/async-await
- **错误边界**: 完善的错误处理避免进程崩溃

### 3. 内存管理
- **流式处理**: 大文件使用流式读取
- **及时释放**: 文件句柄及时关闭
- **垃圾回收**: 避免循环引用和内存泄漏

## 安全考虑

### 1. 配置安全
```json
{
  "security": {
    "api_key_storage": "用户本地文件",
    "config_permissions": "600",
    "no_plain_text_logging": true
  }
}
```

### 2. 文件系统安全
- **路径验证**: 防止路径遍历攻击
- **权限检查**: 确保文件操作权限
- **输入验证**: 文件名和内容安全检查

### 3. 网络安全
- **HTTPS强制**: AI API通信加密
- **超时控制**: 防止长时间阻塞
- **重试机制**: 网络异常时的安全重试

## 部署和分发

### 1. NPM包发布
```json
{
  "package.json": {
    "main": "dist/index.js",
    "bin": {
      "issue-make": "bin/issue-make"
    },
    "files": [
      "dist/**/*",
      "bin/**/*",
      "templates/**/*"
    ]
  }
}
```

### 2. 二进制分发
- **pkg**: 将Node.js应用打包为可执行文件
- **多平台**: 支持Windows、macOS、Linux
- **自包含**: 无需预装Node.js

### 3. 安装方式
```bash
# NPM安装
npm install -g issue-make

# Yarn安装
yarn global add issue-make

# PNPM安装
pnpm add -g issue-make

# 直接运行（开发模式）
npx issue-make
```

## 开发工作流

### 1. 开发环境设置
```bash
# 克隆项目
git clone <repository>
cd issue-make

# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 构建项目
pnpm build

# 运行测试
pnpm test
```

### 2. 代码质量检查
```bash
# 类型检查
pnpm typecheck

# 代码检查
pnpm lint

# 自动修复
pnpm lint:fix

# 代码格式化
prettier --write src/**/*.ts
```

### 3. 测试策略
- **单元测试**: Jest + ts-jest
- **集成测试**: CLI命令端到端测试
- **覆盖率**: 目标80%以上

### 4. 发布流程
```bash
# 版本更新
npm version patch/minor/major

# 构建和测试
pnpm clean && pnpm build && pnpm test

# 发布到NPM
npm publish
```

## 监控和维护

### 1. 错误追踪
- **日志系统**: 结构化日志输出
- **错误报告**: 可选的错误上报机制
- **调试模式**: 开发调试信息

### 2. 性能监控
- **启动时间**: 应用启动性能监控
- **响应时间**: AI服务响应时间
- **资源使用**: 内存和CPU使用情况

### 3. 用户反馈
- **GitHub Issues**: 问题收集和追踪
- **版本更新**: 自动更新检查
- **文档维护**: 持续文档改进

---

## 技术栈总览表

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 运行时 | Node.js | >=16.0.0 | JavaScript运行时 |
| 语言 | TypeScript | 5.2+ | 类型安全开发 |
| TUI | ink | 4.4.1 | React for CLI - 终端UI框架 |
| UI框架 | React | 18.2.0 | React核心库 |
| CLI | commander | 11.1.0 | 命令行解析 |
| AI | openai | 4.20.1 | AI服务集成 |
| 文件系统 | fs-extra | 11.1.1 | 文件操作增强 |
| 数据格式 | yaml | 2.3.4 | YAML处理 |
| 工具库 | date-fns | 2.30.0 | 日期处理 |
| 测试 | jest | 29.7.0 | 单元测试框架 |
| TUI测试 | ink-testing-library | 3.0.1 | TUI组件测试 |
| 代码质量 | eslint + prettier | 最新 | 代码规范 |