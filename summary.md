# Issue-Make 项目总结

## 📋 项目概述

Issue-Make 是一个面向开发者的轻量级 issue 管理工具，支持本地优先、AI 增强和多模态交互（TUI + CLI）。

**核心价值**：
- 本地优先：无需依赖外部服务，快速记录开发想法
- AI 增强：集成 OpenAI SDK，智能生成 issue 标题
- 多模态交互：支持 TUI 全屏界面和 CLI 命令行
- Agent 协作：与 AGENTS.md 集成，支持人机协作开发

---

## 🏗️ 系统架构

### 技术栈

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 运行时 | Node.js | >=16.0.0 | JavaScript 运行时 |
| 语言 | TypeScript | 5.2+ | 类型安全开发 |
| TUI | ink | 4.4.1 | React for CLI - 终端 UI 框架 |
| UI 框架 | React | 18.2.0 | React 核心库 |
| CLI | commander | 11.1.0 | 命令行解析 |
| AI | openai | 4.20.1 | AI 服务集成 |
| 文件系统 | fs-extra | 11.1.1 | 文件操作增强 |
| 数据格式 | yaml | 2.3.4 | YAML 处理 |
| 工具库 | date-fns | 2.30.0 | 日期处理 |
| 测试 | jest | 29.7.0 | 单元测试框架 |
| TUI 测试 | ink-testing-library | 3.0.1 | TUI 组件测试 |

### 架构层次

```
┌─────────────────────────────────────────┐
│              用户接口层                   │
├─────────────────┬───────────────────────┤
│    TUI界面      │      CLI命令行         │
│   (ink/React)   │    (commander)         │
└─────────────────┴───────────────────────┘
┌─────────────────────────────────────────┐
│              业务逻辑层                   │
├─────────────────┬───────────────────────┤
│   Issue管理     │     AI服务集成         │
│   文件流转      │    配置管理             │
└─────────────────┴───────────────────────┘
┌─────────────────────────────────────────┐
│              基础设施层                   │
├─────────────────┬───────────────────────┤
│   文件系统      │      工具函数           │
│ (fs-extra)      │   (date-fns等)         │
└─────────────────┴───────────────────────┘
```

---

## 📁 项目文件结构

```
issue-make/
├── src/                          # 源代码目录
│   ├── cli/                      # CLI 命令模块
│   │   ├── commands/             # 命令实现
│   │   │   ├── init.ts          # 初始化配置命令
│   │   │   ├── add.ts           # 添加 issue 命令
│   │   │   ├── open.ts          # 打开 issue 命令
│   │   │   └── close.ts         # 关闭 issue 命令
│   │   └── index.ts             # CLI 入口
│   ├── tui/                      # TUI 模块
│   │   ├── components/           # React 组件
│   │   │   ├── Welcome.tsx      # 欢迎界面组件
│   │   │   ├── CommandPrompt.tsx # 命令提示组件
│   │   │   ├── Input.tsx        # 输入框组件
│   │   │   ├── Messages.tsx     # 消息显示组件
│   │   │   └── App.tsx          # 主应用组件
│   │   └── app.ts               # TUI 入口
│   ├── core/                     # 核心业务逻辑
│   │   ├── types.ts             # 类型定义
│   │   ├── config.ts            # 配置管理
│   │   ├── ai.ts                # AI 服务
│   │   └── file-manager.ts      # 文件管理
│   ├── utils/                    # 工具函数
│   │   ├── path.ts              # 路径工具
│   │   ├── date.ts              # 日期工具
│   │   └── validation.ts        # 验证工具
│   └── index.ts                 # 主入口
├── tests/                        # 测试目录
│   ├── unit/                     # 单元测试
│   │   ├── config.test.ts       # 配置管理测试
│   │   ├── ai.test.ts           # AI 服务测试
│   │   ├── file-manager.test.ts # 文件管理测试
│   │   ├── cli.test.ts          # CLI 命令测试
│   │   ├── tui.test.ts          # TUI 模块测试
│   │   ├── path.test.ts         # 路径工具测试
│   │   ├── date.test.ts         # 日期工具测试
│   │   └── validation.test.ts   # 验证工具测试
│   └── integration/             # 集成测试
├── bin/                          # 可执行文件
│   └── issue-make               # CLI 入口脚本
├── templates/                    # 模板文件
├── dist/                         # 编译输出目录
├── node_modules/                 # 依赖包
├── package.json                  # 项目配置
├── tsconfig.json                 # TypeScript 配置
├── acceptance-criteria.md        # 验收标准
├── requirements-analysis.md      # 需求分析
├── tech-stack.md                 # 技术栈说明
├── prd.md                        # 产品需求文档
└── summary.md                    # 项目总结（本文件）
```

---

## 📄 文件功能说明

### 核心模块 (src/core/)

#### types.ts
- **功能**：定义所有核心类型和接口
- **主要类型**：
  - `IssueType`：Issue 类型枚举
  - `IssueStatus`：Issue 状态枚举
  - `Settings`：AI 配置接口
  - `IssueFile`：Issue 文件结构
  - `AIResponse`：AI 服务响应
  - `ConfigValidationResult`：配置验证结果

#### config.ts
- **功能**：管理全局配置
- **主要方法**：
  - `getConfig()`：获取配置
  - `setConfig()`：设置配置
  - `ensureConfigDir()`：确保配置目录存在
  - `configExists()`：检查配置是否存在
- **配置路径**：`~/.issue-make/settings.json`

#### ai.ts
- **功能**：AI 服务集成，支持 OpenAI 兼容服务商
- **主要方法**：
  - `generateTitle()`：根据描述生成标题
  - `validateConfig()`：验证配置有效性
  - `isConfigured()`：检查是否已配置
- **降级处理**：AI 不可用时使用时间戳作为默认标题

#### file-manager.ts
- **功能**：管理 issue 文件和目录
- **主要方法**：
  - `createIssue()`：创建新 issue
  - `findIssue()`：查找 issue（支持编号和标题）
  - `openIssue()`：打开 issue（移动到 doing 目录）
  - `closeIssue()`：关闭 issue（归档到 achieved 目录）
  - `getNextId()`：获取下一个可用 ID
- **目录结构**：
  - `.issues/stash/`：待处理 issue
  - `.issues/doing/`：正在处理
  - `.issues/achieved/`：已完成

### CLI 模块 (src/cli/)

#### init.ts
- **功能**：初始化配置
- **执行**：创建配置目录和默认配置文件

#### add.ts
- **功能**：添加新 issue
- **参数**：
  - `--type`：issue 类型
  - `path`：描述文件路径
- **流程**：读取描述 → 生成标题 → 创建 issue 文件

#### open.ts
- **功能**：打开 issue 开始处理
- **参数**：issue 编号或标题
- **流程**：查找 issue → 移动到 doing → 创建 solution.md → 更新 AGENTS.md

#### close.ts
- **功能**：完成并归档 issue
- **参数**：issue 编号或标题
- **流程**：验证 solution.md → 合并内容 → 归档 → 清理 AGENTS.md

### TUI 模块 (src/tui/)

#### App.tsx
- **功能**：主应用组件
- **状态管理**：消息、文件管理器、AI 服务
- **命令处理**：解析 slash 命令，调用相应功能

#### Welcome.tsx
- **功能**：显示欢迎界面和标题

#### CommandPrompt.tsx
- **功能**：显示可用命令和帮助信息

#### Input.tsx
- **功能**：处理用户输入
- **支持**：键盘输入、退格、Ctrl+C 退出

#### Messages.tsx
- **功能**：显示系统消息
- **类型**：info、success、error、warning

### 工具模块 (src/utils/)

#### path.ts
- **功能**：路径相关工具函数
- **主要函数**：
  - `sanitizeTitle()`：清理标题用于文件名
  - `generateIssueFilename()`：生成 issue 文件名
  - `extractIssueNumber()`：提取 issue 编号
  - `getIssuesDir()` 等：获取各目录路径

#### date.ts
- **功能**：日期处理工具
- **主要函数**：
  - `formatDate()`：格式化日期为 YYYY-MM-DD
  - `parseDate()`：解析日期字符串
  - `getCurrentDate()`：获取当前日期
  - `getTimestamp()`：获取时间戳

#### validation.ts
- **功能**：输入验证工具
- **主要函数**：
  - `validateIssueType()`：验证 issue 类型
  - `validateIssueNumber()`：验证 issue 编号
  - `parseSlashCommand()`：解析 slash 命令
  - `isValidUrl()`：验证 URL 格式

---

## 🎯 核心功能实现

### 1. Issue 生命周期

```
创建 (add) → 待处理 (stash) → 处理中 (doing) → 已完成 (achieved)
```

**状态流转**：
1. **创建**：通过 CLI 或 TUI 创建 issue，存放在 `stash/` 目录
2. **打开**：执行 `open` 命令，移动到 `doing/`，创建 `solution.md`
3. **完成**：执行 `close` 命令，合并 solution，归档到 `achieved/`

### 2. AI 标题生成

**流程**：
1. 检查 AI 配置是否完整
2. 调用 OpenAI API 生成标题
3. 清理和优化生成的标题
4. 失败时降级为时间戳标题

**支持的 AI 服务商**：
- OpenAI
- Deepseek
- 所有 OpenAI 兼容的服务商

### 3. AGENTS.md 集成

**标记符**：
```html
<!-- ISSUE-MAKE:START -->
[任务描述和解决方案模板]
<!-- ISSUE-MAKE:END -->
```

**工作流**：
- `open` 时：插入任务描述和解决方案模板
- `close` 时：清理 ISSUE-MAKE 标记内容

### 4. 文件命名规范

- **stash/doing**：`{sanitized-title}.{number}.md`
- **achieved**：`{sanitized-title}.md`
- **日期格式**：`YYYY-MM-DD`

---

## 💡 使用案例

### 案例 1：创建并处理 Feature Issue

#### 步骤 1：初始化配置
```bash
issue-make init
```

#### 步骤 2：配置 AI（可选）
编辑 `~/.issue-make/settings.json`：
```json
{
  "url": "https://api.deepseek.com",
  "api": "sk-xxx",
  "model": "deepseek-chat"
}
```

#### 步骤 3：创建描述文件
创建 `feature-desc.md`：
```markdown
添加用户认证功能，支持 JWT token 验证和用户注册登录。
```

#### 步骤 4：添加 issue
```bash
issue-make add --type feat feature-desc.md
```

**输出**：
```
✓ Issue created successfully
  ID: 0
  Title: Add User Authentication with JWT
  Type: feat
  File: .issues/stash/Add-User-Authentication-with-JWT.0.md
```

#### 步骤 5：打开 issue
```bash
issue-make open 0
```

**结果**：
- Issue 移动到 `.issues/doing/`
- 创建 `.issues/solution.md`
- 更新 `AGENTS.md` 添加任务描述

#### 步骤 6：实现解决方案
编辑 `.issues/solution.md`：
```markdown
# Solution for Issue #0: Add User Authentication with JWT

## 实现步骤
1. 安装依赖：jsonwebtoken, bcrypt
2. 创建用户模型
3. 实现注册 API
4. 实现登录 API
5. 添加 JWT 中间件

## 代码实现
[具体代码...]
```

#### 步骤 7：完成 issue
```bash
issue-make close 0
```

**结果**：
- 合并 solution 到 issue 文件
- 归档到 `.issues/achieved/`
- 清理 `AGENTS.md` 和 `solution.md`

---

### 案例 2：使用 TUI 快速记录 Bug

#### 启动 TUI
```bash
issue-make
```

#### 交互流程
```
╔════════════════════════════════════════╗
║        Issue-Make TUI v1.0.0        ║
╚════════════════════════════════════════╝

Available commands:
  /add:feat    - Add a new feature issue
  /add:todo    - Add a new todo issue
  /add:bug     - Add a new bug issue
  /add:refact  - Add a new refactor issue
  /exit        - Exit TUI

> /add:bug
```

**输入描述**：
```
登录页面在移动端显示错位，按钮无法点击
```

**结果**：
```
✓ Issue #1 created: Mobile Login Layout Issue
  File: .issues/stash/Mobile-Login-Layout-Issue.1.md
```

---

### 案例 3：批量管理 Tasks

#### 创建多个 todo
```bash
# 创建描述文件
echo "完成用户注册功能" > todo-1.md
echo "实现密码重置" > todo-2.md
echo "添加邮箱验证" > todo-3.md

# 批量添加
issue-make add --type todo todo-1.md
issue-make add --type todo todo-2.md
issue-make add --type todo todo-3.md
```

#### 查看所有 issues
```bash
ls .issues/stash/
```

**输出**：
```
Complete-user-registration.0.todo.md
Implement-password-reset.1.todo.md
Add-email-verification.2.todo.md
```

#### 按顺序处理
```bash
issue-make open 0    # 开始第一个任务
# ... 完成任务 ...
issue-make close 0   # 完成第一个任务

issue-make open 1    # 开始第二个任务
# ... 完成任务 ...
issue-make close 1

issue-make open 2    # 开始第三个任务
# ... 完成任务 ...
issue-make close 2
```

---

### 案例 4：使用模糊标题匹配

```bash
# 创建 issues
issue-make add --type feat desc1.md  # 生成标题 "Add User Profile"
issue-make add --type feat desc2.md  # 生成标题 "Add User Settings"

# 使用部分标题打开
issue-make open "User Profile"  # 打开第一个
issue-make open "Profile"       # 也能打开第一个
issue-make open "Settings"      # 打开第二个
```

---

### 案例 5：AI 降级处理

#### 场景：网络不可用
```bash
# AI 服务未配置或网络断开
issue-make add --type feat description.md
```

**输出**：
```
⚠ AI not configured, using fallback title
✓ Issue created successfully
  ID: 0
  Title: Issue-1705051200000
  Type: feat
  File: .issues/stash/Issue-1705051200000.0.md
```

**说明**：系统自动使用时间戳作为标题，不影响功能使用。

---

## 🧪 测试覆盖

### 单元测试
- **config.test.ts**：配置管理测试（AC-1.1.1, AC-1.1.2, AC-1.2.1, AC-1.2.2）
- **ai.test.ts**：AI 服务测试（AC-2.3.1, AC-2.3.2, AC-6.2.1, AC-6.2.2）
- **file-manager.test.ts**：文件管理测试（AC-3.1.1, AC-3.1.2, AC-3.2.1, AC-3.2.2, AC-3.3.1 等）
- **cli.test.ts**：CLI 命令测试（AC-3.1.1, AC-3.1.2, AC-3.2.1, AC-3.2.2, AC-3.3.1）
- **tui.test.ts**：TUI 模块测试（AC-2.1.1, AC-2.1.2, AC-2.2.1, AC-2.2.2, AC-2.2.3 等）
- **path.test.ts**：路径工具测试（AC-4.3.1, AC-4.3.2）
- **date.test.ts**：日期工具测试（AC-4.3.3）
- **validation.test.ts**：验证工具测试（AC-2.2.1, AC-2.2.2, AC-3.2.1, AC-3.2.2）

### 运行测试
```bash
# 运行所有测试
npm test

# 运行特定测试
npm test -- config.test.ts

# 生成覆盖率报告
npm run test:coverage
```

---

## 📊 验收标准完成情况

### P0 级别（必须通过）
- ✅ AC-1.1.1: 配置文件创建
- ✅ AC-2.1.1: TUI 启动
- ✅ AC-2.2.1: Slash 命令解析
- ✅ AC-2.2.3: slash 命令文件存储在 stash 目录
- ✅ AC-3.1.1: CLI add 命令
- ✅ AC-3.2.1: open by number
- ✅ AC-4.1.1: 目录创建
- ✅ AC-4.2.1: ID 自增
- ✅ AC-4.3.3: issue 文件日期格式正确
- ✅ AC-5.1.1: AGENTS.md 更新
- ✅ AC-5.1.3: open 时创建 solution.md 文件
- ✅ AC-5.1.4: AGENTS.md 使用 XML 注释标记符
- ✅ AC-6.3.1: 重复 open 同一 issue 报错
- ✅ AC-6.3.2: close 时缺少 solution.md 报错

### P1 级别（重要）
- ✅ AC-1.2.1: AI 配置验证
- ✅ AC-2.3.1: AI 标题生成
- ✅ AC-3.2.2: open by title
- ✅ AC-3.3.1: close 流程
- ✅ AC-4.3.1: 文件命名规范
- ✅ AC-5.1.2: AGENTS.md 不存在时自动创建
- ✅ AC-5.2.1: AGENTS.md 清理

### P2 级别（期望）
- ✅ AC-1.2.2: 配置错误提示
- ✅ AC-2.2.2: 命令错误提示
- ✅ AC-2.3.2: AI 降级处理
- ✅ AC-4.2.2: 删除后 ID 管理
- ✅ AC-6.1.1: 权限异常处理
- ✅ AC-6.2.1: 网络异常降级

---

## 🚀 快速开始

### 安装
```bash
npm install
```

### 构建
```bash
npm run build
```

### 使用
```bash
# CLI 模式
node dist/index.js init
node dist/index.js add --type feat description.md
node dist/index.js open 0
node dist/index.js close 0

# TUI 模式
node dist/index.js

# 开发模式
npm run dev
```

---

## 📝 总结

Issue-Make 是一个功能完整的 issue 管理工具，具有以下特点：

**技术亮点**：
- 使用 Ink (React for CLI) 构建现代化的 TUI 界面
- TypeScript 提供完整的类型安全
- 完善的单元测试覆盖
- 模块化设计，易于扩展

**功能特性**：
- 支持 CLI 和 TUI 双模式交互
- AI 智能生成 issue 标题
- 与 AGENTS.md 无缝集成
- 完整的 issue 生命周期管理
- 灵活的文件命名和状态流转

**适用场景**：
- 个人开发任务管理
- Bug 跟踪和记录
- Feature 需求收集
- 重构任务规划
- 与 AI Agent 协作开发

项目已通过所有 P0 和 P1 级别的验收标准，可以投入实际使用。